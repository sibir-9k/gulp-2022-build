// Копировать в новый проект файлы 
gulp, src(папка), gulpfile, package.json

// В новом проекте установить зависимости(пакеты из package.json) 
npm i

// Запуск проекта в режиме разработчика (лайт версия для быстрого запуска 
   без оптимизаций и сжатий картинок)
npm run dev

// Собрать готовый проект (режим продакшина)
npm run build